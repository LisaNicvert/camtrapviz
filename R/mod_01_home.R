# UI ----------------------------------------------------------------------

homeUI <- function(id) {
  tagList(
    p("Home tab")
  )
}

# Server ----------------------------------------------------------------------

homeServer <- function(id) {
  moduleServer(id, function(input, output, session) {
    
  })
}